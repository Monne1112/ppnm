set terminal png
set output "ann.png"

set xlabel "x"
set ylabel "y"

plot \
    "points.data" using 1:2 with points pt 7 title "training points", \
    "ann.data" using 1:2 with lines title "ANN", \
    "ann.data" using 1:3 with lines title "target"